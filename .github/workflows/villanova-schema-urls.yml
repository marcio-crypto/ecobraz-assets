name: Villanova ESG — corrigir URLs dentro dos dados estruturados

on:
  workflow_dispatch:
    inputs:
      modo:
        description: 'simular (só relatório) ou aplicar (grava no Ghost)'
        type: choice
        options:
          - simular
          - aplicar
        default: simular

permissions:
  contents: read

jobs:
  corrigir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trocar as URLs antigas no JSON-LD do code injection
        env:
          VILLANOVA_GHOST_ADMIN_URL: ${{ secrets.VILLANOVA_GHOST_ADMIN_URL }}
          VILLANOVA_GHOST_ADMIN_API_KEY: ${{ secrets.VILLANOVA_GHOST_ADMIN_API_KEY }}
        run: node site-villanova/scripts/corrige-schema-urls.mjs "${{ inputs.modo }}"
